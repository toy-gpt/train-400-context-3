# ============================================================
# .github/workflows/release-pypi.yml
# ============================================================
# WHY: Calls org-level deploy;
# maintained centrally in toy-gpt/.github.

name: Release PyPI

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: toy-gpt/.github/.github/workflows/release-pypi-shared.yml@main
